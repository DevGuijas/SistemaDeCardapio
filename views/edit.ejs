<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Item - Rancho do Cupim</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Special+Elite&display=swap" rel="stylesheet">
    <style>
        :root {
            --madeira: #3d2b1f;
            --papel: #f4e4bc;
            --detalhe: #8b0000;
        }

        body {
            background-color: var(--madeira);
            background-image: url('https://www.transparenttextures.com/patterns/dark-wood.png');
            font-family: 'Special Elite', cursive;
            display: flex;
            justify-content: center;
            padding: 40px 20px;
        }

        .form-container {
            background-color: var(--papel);
            padding: 30px;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            border-radius: 5px;
            border: 5px solid #d2b48c;
        }

        h2 {
            font-family: 'Playfair Display', serif;
            color: var(--madeira);
            text-align: center;
            border-bottom: 2px solid var(--madeira);
            padding-bottom: 10px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: var(--madeira);
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"],
        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #b5a47a;
            background-color: rgba(255, 255, 255, 0.5);
            font-family: 'Special Elite', cursive;
            box-sizing: border-box;
        }

        .preview-img {
            width: 100%;
            height: 150px;
            object-fit: cover;
            margin-bottom: 10px;
            border: 2px solid var(--madeira);
        }

        .btn-salvar {
            background-color: #2d5a27;
            color: white;
            padding: 12px;
            border: none;
            width: 100%;
            cursor: pointer;
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            transition: 0.3s;
        }

        .btn-salvar:hover {
            background-color: #1e3d1a;
        }

        .btn-voltar {
            display: block;
            text-align: center;
            margin-top: 15px;
            color: var(--detalhe);
            text-decoration: none;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>

    <div class="form-container">
        <h2>Editar Especialidade</h2>

        <form action="/admin/edit/<%= item._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
            
            <div class="form-group">
                <label>Foto Atual:</label>
                <img src="/uploads/<%= item.imagem %>" class="preview-img">
                <label for="foto">Trocar Foto (deixe vazio para manter):</label>
                <input type="file" name="foto" id="foto">
            </div>

            <div class="form-group">
                <label for="titulo">Título do Prato:</label>
                <input type="text" name="titulo" id="titulo" value="<%= item.titulo %>" required>
            </div>

            <div class="form-group">
                <label for="descricao">Descrição:</label>
                <textarea name="descricao" id="descricao" rows="4" required><%= item.descricao %></textarea>
            </div>

            <div class="form-group">
                <label for="preco">Preço (R$):</label>
                <input type="number" name="preco" id="preco" step="0.01" value="<%= item.preco %>" required>
            </div>

            <button type="submit" class="btn-salvar">ATUALIZAR NO CARDÁPIO</button>
        </form>

        <a href="/admin" class="btn-voltar">← Voltar para o Painel</a>
    </div>

</body>
</html>