<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Rancho do Cupim - Cardápio</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Special+Elite&display=swap" rel="stylesheet">
    <style>
        :root { --madeira: #3d2b1f; --papel: #f4e4bc; --detalhe: #8b0000; }
        body { background-color: var(--madeira); background-image: url('https://www.transparenttextures.com/patterns/dark-wood.png'); font-family: 'Special Elite', cursive; padding: 20px; }
        .container { max-width: 800px; margin: 0 auto; background-color: var(--papel); padding: 40px; box-shadow: 0 0 50px #000; border: 10px double var(--madeira); }
        header { text-align: center; border-bottom: 2px solid var(--madeira); margin-bottom: 30px; padding-bottom: 10px; }
        .cat-titulo { font-family: 'Playfair Display'; color: var(--madeira); border-bottom: 1px solid var(--madeira); margin-top: 40px; text-transform: uppercase; }
        .item-cardapio { display: flex; margin-bottom: 20px; padding-bottom: 15px; border-bottom: 1px dashed #b5a47a; align-items: center; }
        
        /* Estilo para item ESGOTADO */
        .esgotado { opacity: 0.5; filter: grayscale(1); position: relative; }
        .badge-esgotado { background: var(--detalhe); color: white; padding: 5px 10px; font-size: 0.8rem; position: absolute; top: 10px; right: 10px; transform: rotate(5deg); border: 2px solid white; }

        .item-foto { width: 120px; height: 120px; object-fit: cover; border: 2px solid var(--madeira); }
        .item-info { flex: 1; padding-left: 20px; }
        .item-preco { color: #2d5a27; font-weight: bold; font-size: 1.2rem; }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1 style="font-family: 'Playfair Display'; font-size: 3rem; margin:0; color: var(--madeira);">Rancho do Cupim</h1>
            <p>O autêntico sabor da brasa</p>
        </header>
        
        <% const categorias = ['Carnes', 'Acompanhamentos', 'Bebidas'] %>
        <% categorias.forEach(cat => { %>
            <% const itensDaCat = itens.filter(i => i.categoria === cat) %>
            <% if(itensDaCat.length > 0) { %>
                <h2 class="cat-titulo"><%= cat %></h2>
                <% itensDaCat.forEach(item => { %>
                    <div class="item-cardapio <%= item.disponivel ? '' : 'esgotado' %>">
                        <% if(!item.disponivel) { %>
                            <span class="badge-esgotado">ESGOTADO POR HOJE</span>
                        <% } %>
                        <img src="/uploads/<%= item.imagem %>" class="item-foto">
                        <div class="item-info">
                            <h3 style="margin:0; color: var(--detalhe);"><%= item.titulo %></h3>
                            <p style="margin: 5px 0; color: #555; font-size: 0.9rem;"><%= item.descricao %></p>
                            <span class="item-preco">R$ <%= item.preco.toFixed(2).replace('.', ',') %></span>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        <% }) %>

        <% if(itens.length === 0) { %>
            <p style="text-align: center;">Nossa brasa está sendo preparada. Volte logo!</p>
        <% } %>
        <footer>
            <p>&copy; 2025 Rancho do Cupim - Carne de qualidade e brasa quente.</p>
        </footer>
    </div>
</body>
</html>